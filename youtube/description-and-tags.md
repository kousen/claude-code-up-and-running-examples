# Description + Tags — "One Bash Script Runs Claude Code on Any OpenRouter Model"

Keyword-researched with vidIQ (seed: "claude code", ~4.59M searches/mo, vol 99 / comp 66).
Head term is too competitive to rank on directly — these target the beatable long-tail.

---

## YouTube description

> First two lines are above the fold — keyword-rich + hook. Keep the key sentence in line 1.

```
Claude Code doesn't have to run on Claude. Here's how to use Claude Code with any model on
OpenRouter — DeepSeek, GLM, Kimi, even the openrouter/auto and openrouter/free
routers — all from one tiny bash wrapper.

The trick: Claude Code reads ANTHROPIC_BASE_URL, so you point it at OpenRouter's
Anthropic-compatible endpoint, hand it your OpenRouter key, and pass the model slug straight
through as --model. The harness never knows the difference. I run the SAME coding task through each model so you can
judge them yourself — no leaderboard, no hype. I also show the one real catch: when you use a
router, Claude Code won't tell you which model it actually picked (but the OpenRouter dashboard
will). For a project that should always use one routed backend, .claude/settings.local.json can
persist the same env vars; the script is for fast model switching.

This ties into my book, Claude Code: Up and Running — the OpenRouter material lands in Chapter 12,
coming to the early release later. The orclaude script is in the book's example repo (link below).

⏱️ Chapters
00:00  Claude Code, running on a model that isn't Claude
00:30  Why you'd swap the model
01:30  How it works (ANTHROPIC_BASE_URL + OpenRouter)
03:30  The task every model gets
04:00  GLM, DeepSeek, and Kimi (naming the model)
07:??  openrouter/auto — and the routed-model mystery
08:??  openrouter/free — running for $0
09:30  The honest verdict: harness vs. model
11:00  Book tie-in + what to try next

🔗 Links
• orclaude script (example repo): <REPO LINK>
• Claude Code: Up and Running (early release): <BOOK LINK>
• OpenRouter models: https://openrouter.ai/models

#claudecode #openrouter #aicoding
```

> Fill in `<REPO LINK>` / `<BOOK LINK>` and the real timestamps after editing.
> Re-time the chapters once the final cut exists.

---

## Tags

Built from vidIQ keyword research (high-volume / beatable-competition picks) + tags that recur on
the breakout "claude code" videos. YouTube allows ~500 chars total — trim from the bottom if over.

```
claude code, how to use claude code, claude code tutorial, claude code openrouter, claude code any model,
claude code free, openrouter, openrouter auto, openrouter free, claude code setup, claude code install,
claude code tricks, claude code workflow, run claude code on any model, deepseek, deepseek v4, glm 5.2,
kimi, ai coding assistant, anthropic claude, free ai models, ai coding tools,
ANTHROPIC_BASE_URL, ken kousen, claude code up and running
```

### Why these (keyword data)
| Tag source | Vol | Comp | Verdict |
|---|---:|---:|---|
| how to use claude code | 85 | 43 | best ratio — keep high |
| claude code install | 67 | 25 | low comp, easy reach |
| claude code tricks | 63 | 29 | low comp, on-brand |
| claude code tutorial | 89 | 51 | high vol, one mention worth it |
| claude code workflow | 74 | 46 | secondary |

---

## Strategy notes (from the outliers scan)

- **FREE angle is proven.** A 6K-sub channel hit 20K views / breakout score 98 with "I Got Claude
  Opus 4.8 for FREE in Claude Code." Promote the `openrouter/free` run to a headline beat, not slot #2.
  ("FREE" tests very differently from "for pennies/trick", which your channel scored at 70.)
- **You have a content gap.** Nobody in the 3-month "claude code" outliers runs the full "any model,
  one script" premise — the closest is that single FREE-Opus video. Yours is broader and more technical.
- **Don't chase the viral patterns.** "Build $10K websites", "tool + tool = X" dominate breakouts but
  are off-brand. Stay in the honest-technical lane that fits the channel.
- **Thumbnail-proven words:** FREE, ANY MODEL. Pair with model logos (DeepSeek / GLM / Kimi).

## Title A/B (re-score on record day)
- A: One Bash Script Runs Claude Code on Any OpenRouter Model  (score_title 89, channel-grounded)
- B: One Bash Script Unlocks Claude on Any Model  (generate_titles 91, region-grounded — not directly comparable)
- Avoid: anything with "broke walls / bypassed limits / shocking" — misleading + clickbait-coded.
