# weather-app

A small Flask weather service — the "cabin" project. Modest in size but
real enough to touch the web, parse user input, manage dependencies, and
hold an external API key.

## Chapter phases

| Folder | Chapter | Focus |
| --- | --- | --- |
| `ch05-permissions/` | Ch. 5 — Permissions, Safety, Trust Boundaries | Allow/deny lists, secrets, branch-first |

The app may also be referenced in Ch. 9 (test-first refactoring) — a
chapter-specific subfolder will appear here if a distinct version is needed.
