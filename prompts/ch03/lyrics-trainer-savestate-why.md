# Chapter 3 lyrics-trainer "why this way?" prompt

Used after the local-persistence run, for the "Ask Claude Code: Why this way?"
sidebar. A follow-up question about a design choice in the generated code.

```text
The implementation put localStorage.setItem(...) directly in the next-button handler. Why not create a saveState() function?
```
