# prompts

Reusable prompts referenced in the book, organized by chapter
(e.g. `ch02/`, `ch03/`, …).

Each file is a self-contained prompt the reader can copy into a Claude Code
session. Filenames should describe the prompt's purpose, not its position
in the chapter (so they stay stable if the text is reorganized).
